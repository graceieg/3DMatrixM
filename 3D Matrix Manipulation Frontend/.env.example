# API Configuration
VITE_API_BASE_URL=http://localhost:8000

# Note: Create a .env file in the same directory and set your actual API URL
# The .env file is in .gitignore and should not be committed to version control
